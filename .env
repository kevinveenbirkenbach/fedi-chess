# ------------------------------------------------------------
# fedi-chess (castling.club) - example environment
# Copy to .env and adjust values.
# ------------------------------------------------------------

# App basics
APP_SCHEME=http
APP_DOMAIN=localhost
APP_ADMIN_URL=
APP_ADMIN_EMAIL=admin@example.org

# Signing / security
# IMPORTANT: This file (and its .pub) must be writable by the container user.
APP_KEY_FILE=/app/data/signing-key
APP_HMAC_SECRET=change-me-to-a-long-random-string

# Node
NODE_ENV=production
PORT=5080

# PostgreSQL (libpq envs)
PGHOST=postgres
PGPORT=5432
PGDATABASE=chess
PGUSER=chess
PGPASSWORD=change-me
